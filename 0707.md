# 2026-07-07 Beam iOS: Batch → Realtime 전환 TODO

## RunPod / 원격 SVC 세션 (2026-07-07)

### 지금까지 된 것

오전 smoke test **성공** (GPU 직접 + Nest 게이트웨이 둘 다):

| 항목 | 결과 |
|------|------|
| RunPod SVC URL | `http://213.173.102.23:37723` |
| `GET /ai-convert/health` | `ok: true`, `modelsLoaded: true` |
| `GET /ai-convert/voices` | 2개 (`dionn_v1_singing`, `freya_idol`) |
| async job | `processing` → `completed`, MP3 ~1.8MB 다운로드 OK |
| Nest proxy | `beam-server-nest/.env` → `BEAM_SVC_URL` 연결 OK |
| 로컬 Nest `:8080` | health/voices 프록시 동작 확인 (2026-07-07 11:28) |

주의: health 응답에 `"gpu": false` — **모델은 로드됐지만 GPU 미사용(CPU 추론)** 상태일 수 있음. 변환은 되지만 느릴 수 있음.

### 구조 (현재)

```text
iOS App
  → beam-server-nest (로컬 :8080)
    → BEAM_SVC_URL = http://213.173.102.23:37723
      → beam-svc-server (RunPod)
```

iOS는 SVC URL을 직접 치지 않고 **Nest만** 호출하면 됨.

### 이어서 할 것 (RunPod + iOS E2E)

- [ ] RunPod에서 `gpu: true` 확인 (CUDA/드라이버, `BEAM_RVC_DEVICE=cuda:0`)
- [ ] iOS 시뮬레이터 `BEAM_API_BASE_URL` = `http://127.0.0.1:8080` 확인
- [ ] 앱에서 Voice 버튼 → preview 변환 → 재생 E2E 테스트
- [ ] pre-conversion이 재생 시작 후 백그라운드에서 job 도는지 확인
- [ ] 변환 결과가 실제로 목소리가 바뀌는지 청취 확인 (stub 아닌 RVC 동작)
- [ ] RunPod IP/포트 바뀌면 `.env` + 문서 URL 갱신

점검 커맨드:

```bash
# RunPod 직접
curl http://213.173.102.23:37723/ai-convert/health
curl http://213.173.102.23:37723/ai-convert/voices

# 로컬 Nest 경유
curl http://127.0.0.1:8080/ai-convert/health

# 전체 smoke (mp3 파일 필요)
GPU_BASE_URL=http://213.173.102.23:37723 \
NEST_BASE_URL=http://127.0.0.1:8080 \
./Beam-iOS/scripts/beam-connectivity-check.sh /path/to/input.mp3
```

---

## E2E 테스트 (2026-07-07)

### 백엔드 경로 검증 (CLI, iOS와 동일 API)

iOS Voice 버튼이 호출하는 경로를 Nest 경유로 재현:

```bash
# 1) health / voices
curl http://127.0.0.1:8080/ai-convert/health
curl http://127.0.0.1:8080/ai-convert/voices

# 2) preview 변환 (iOS와 동일: full upload + server trim)
BASE_URL=http://127.0.0.1:8080 \
TRIM_START=0 TRIM_DURATION=30 \
VOICE_ID=dionn_v1_singing VOICE_TYPE=singer \
OUT_FILE=/tmp/beam_e2e_preview.mp3 \
beam-server-nest/scripts/smoke_test.sh /path/to/input.mp3

# 3) full track async (백그라운드 job)
BASE_URL=http://127.0.0.1:8080 RETURN_JOB=true \
VOICE_ID=dionn_v1_singing \
OUT_FILE=/tmp/beam_e2e_full.mp3 \
beam-server-nest/scripts/smoke_test.sh /path/to/input.mp3
```

**2026-07-07 결과:**

| 단계 | 결과 | 소요 |
|------|------|------|
| health / voices (Nest → RunPod) | OK | 즉시 |
| preview (30초 trim, sync) | OK, 705KB MP3 | **~4분 45초** (`gpu: false` CPU) |
| full async job | 진행 중 / 별도 확인 | 수 분~ |

주의: RunPod `gpu: false` 이면 앱에서 Voice 버튼 눌러도 **수 분 기다려야** preview가 나올 수 있음.

### iOS 시뮬레이터 E2E 절차

**사전 조건**

- [x] `beam-server-nest` 로컬 `:8080` 실행 중
- [x] `BEAM_SVC_URL=http://213.173.102.23:37723` (RunPod)
- [ ] PostgreSQL 실행 + Nest DB 연결 (로그인/pre-conversion용)
- [ ] Xcode에서 `Beam-iOS/BeamApp.xcodeproj` → iPhone Simulator 실행

**URL (시뮬레이터 기본값, 수정 불필요)**

- `Endpoints.baseURL` → `http://127.0.0.1:8080` (Info.plist 없어도 fallback)

**테스트 A — Voice 버튼 (직접 변환, 로그인 불필요)**

1. 앱 실행 → 곡 재생 (데모 `Fix You` 또는 Jamendo 곡)
2. 플레이어에서 **Voice** 버튼 탭
3. `dionn_v1_singing` 선택
4. 로딩 오버레이: `현재 위치부터 최대 1분 미리듣기...`
5. **~5분 대기** (CPU 모드) 후 변환된 목소리로 재생 시작
6. Xcode 콘솔 확인:
   - `🌐 Beam SVC convert URL: http://127.0.0.1:8080/ai-convert/voice-conversion`
   - `✂️ Using server-side preview trim...`
   - `🎯 Telemetry` / playback started

**테스트 B — Pre-conversion (로그인 필요)**

1. 회원가입 → 이메일 인증 → 로그인
2. 곡 재생 시작 → `onChange(currentTrack)` → `preConversionManager.warmup()` 자동 호출
3. Voice 버튼 탭 시 cache/pre-conversion hit면 **즉시 재생**
4. 콘솔: `🎯 Telemetry Cache Hit!` 또는 `⏳ Warmup Job In-Progress`

**실패 시 체크**

| 증상 | 원인 |
|------|------|
| 음성 목록 fallback만 보임 | Nest `:8080` 미실행 또는 `/ai-convert/voices` 실패 |
| HTTP 500 / timeout | RunPod SVC down, 또는 CPU 변환 중 (5분+ 대기) |
| pre-conversion 안 됨 | 미로그인 (`TokenStorage` nil) |
| 원곡과 소리 동일 | RVC stub/실패 — RunPod 로그 확인 |

### E2E 체크리스트

- [x] Nest → RunPod health/voices
- [x] Nest preview conversion (sync + trim)
- [ ] Nest full async job 완료
- [ ] iOS 시뮬레이터 Voice 버튼 → preview 재생
- [ ] iOS full track 자동 교체
- [ ] iOS pre-conversion (로그인 후) instant playback
- [ ] RunPod GPU 활성화 후 TTFA 재측정


- [x] pre-conversion용 preferred voice 저장
- [x] 현재 재생 중인 트랙 기준 auto warmup 훅 추가
- [x] Voice 선택 시 다음 트랙부터 미리 준비할 수 있게 연결
- [x] iOS voice conversion 경로를 beam-server gateway 기준으로 정리
- [x] audio cache + cache hit/miss 로그
- [x] iOS telemetry log formatting 정리
- [x] preview first / full async 정리
- [x] server-side trim proxy 정리
- [x] 실행용 connectivity check 스크립트 추가
- [ ] streaming PoC

## beam-server / beam-svc-server 체크리스트

실행 커맨드:

```bash
# 한 번에 둘 다 점검
./scripts/beam-connectivity-check.sh /path/to/input.mp3

# GPU 서버 직접 확인
BASE_URL=http://127.0.0.1:8081 ../beam-svc-server/scripts/smoke_test.sh /path/to/input.mp3

# Nest 게이트웨이 확인
BASE_URL=http://127.0.0.1:8080 RETURN_JOB=true ../beam-server-nest/scripts/smoke_test.sh /path/to/input.mp3
```

- [x] `beam-svc-server`가 GPU 서버(RunPod)에서 실행 중인지 확인
- [x] `GET /ai-convert/health` 가 GPU 서버에서 200 응답하는지 확인
- [x] `GET /ai-convert/voices` 가 GPU 서버에서 보이스 목록을 주는지 확인
- [x] `beam-server-nest/.env` 의 `BEAM_SVC_URL` 이 GPU 서버 주소를 가리키는지 확인 (`http://213.173.102.23:37723`)
- [x] `beam-server-nest` 재시작 후 `GET /ai-convert/health` 가 Nest 서버를 통해 동작하는지 확인
- [x] `GET /ai-convert/voices` 가 Nest 서버를 통해 동작하는지 확인
- [x] smoke test: async job submit → poll → MP3 download 성공 (2026-07-07)
- [ ] `./scripts/beam-connectivity-check.sh /path/to/input.mp3` 로 로컬에서 한 번 더 점검 (mp3 파일 필요)
- [ ] RunPod GPU 활성화 (`gpu: true`) 확인
- [x] iOS는 `BEAM_API_BASE_URL` 만 사용하고, `BEAM_SVC_BASE_URL` 직접 의존은 최소화
- [x] 실패 시 Nest 로그에 upstream 에러가 보이도록 정리

## 지금 우리가 하려는 것

Beam Music은 현재 **batch 방식**으로 동작한다.

현재 흐름:

```text
노래 파일 준비
→ 필요한 구간을 자름
→ 서버에 업로드
→ 서버가 보컬 변환
→ 변환된 파일을 다운로드
→ iOS에서 새 파일로 재생
```

이 방식은 구현은 안정적이지만, 사용자가 Voice 버튼을 눌렀을 때 **첫 소리가 나오기까지 기다리는 시간**이 길다.

목표는 단순히 “전체 변환 시간을 무조건 줄이는 것”이 아니다.  
3분짜리 노래를 AI로 바꾸려면 어차피 3분 분량의 inference는 필요하다.

우리가 줄이려는 핵심은:

> Voice 버튼을 눌렀을 때 사용자가 기다린다고 느끼는 시간, 즉 **첫 변환 음원이 들리기까지의 시간(TTFA)**

---

## 쉬운 비유

- 현재 batch 방식: 사진을 찍어서 사진관에 맡기고, 보정이 끝난 사진을 다시 받는 방식
- realtime 방식: 카메라에 라이브 필터를 켜서 바로바로 바뀐 화면을 보는 방식

Beam의 현실적인 방향은 바로 완전 realtime으로 갈아엎기보다:

1. **미리 변환(pre-conversion)** 해서 Voice 버튼을 누르기 전에 준비해두고
2. **짧은 preview를 먼저 재생**하고
3. **전체 곡은 백그라운드에서 변환**하며
4. 이후 필요하면 진짜 streaming realtime 구조로 확장하는 것

---

## 큰 전략

### 1단계: 지금 batch 구조를 더 빠르게 느껴지게 만들기

완전 realtime 전에 먼저 해야 할 것:

- 오디오 다운로드 캐시
- 변환 결과 캐시
- pre-conversion
- 15~60초 preview 우선 변환
- full track async 변환
- Voice 버튼 클릭 시 즉시 재생 가능한 구조

### 2단계: streaming realtime R&D

진짜 realtime은 서버와 iOS 모두 구조 변경이 크다.

필요한 것:

- chunk 단위 오디오 처리
- WebSocket 또는 streaming HTTP
- 서버 GPU session 유지
- iOS에서 chunk buffer 재생
- chunk 경계 crossfade / overlap 처리
- seek 대응
- 실패 시 batch fallback

---

## TODO List

## Phase 0. 현재 구조 파악

- [ ] 현재 Voice 버튼을 눌렀을 때 실행되는 iOS flow 확인
  - 어떤 ViewModel / Manager가 conversion을 시작하는지
  - preview 변환과 full 변환이 이미 분리되어 있는지
  - 변환 완료 후 어떤 player로 교체하는지
- [ ] 현재 서버 API 확인
  - input: full audio file인지, trimmed audio인지
  - output: mp3/wav/m4a 파일 URL인지
  - progress/status API가 있는지
- [ ] 현재 대기 시간 측정
  - Voice tap time
  - audio fetch start/end
  - trim/export start/end
  - upload start/end
  - server conversion start/end
  - download start/end
  - playback start
- [x] TTFA 지표 추가
  - `voice_tap_to_first_audio_ms`
  - `conversion_request_to_result_ms`
  - `cache_hit_rate`
  - `preconversion_ready_rate`

---

## Phase 1. Batch 유지하면서 체감 대기 줄이기

### 1-1. Audio Cache

- [x] 같은 곡을 다시 재생할 때 원본 오디오를 재다운로드하지 않도록 캐시
- [x] cache key 정의
  - trackId
  - source URL
  - duration
  - updatedAt 또는 checksum
- [x] 캐시 만료 정책 추가
  - max disk size
  - LRU 삭제
- [x] 캐시 hit/miss 로그 추가

목표:

> 같은 곡 재생 시 fetch 시간 2~10초 절약

---

### 1-2. Conversion Result Cache

- [x] 같은 곡 + 같은 voiceId + 같은 구간이면 변환 결과 재사용
- [x] cache key 정의
  - trackId
  - voiceId
  - startTime
  - duration
  - modelVersion
- [x] preview 결과와 full track 결과를 분리해서 저장
- [x] Voice 버튼 클릭 시 먼저 cache 확인
- [x] cache hit이면 서버 요청 없이 즉시 재생

목표:

> 두 번째 재생부터 거의 즉시 Voice playback

---

### 1-3. Pre-conversion Manager 강화

핵심 아이디어:

```text
사용자가 노래를 재생하기 시작함
→ iOS가 백그라운드에서 미리 preview 변환 요청
→ 사용자가 Voice 버튼을 누름
→ 이미 변환된 결과가 있으면 즉시 재생
```

- [ ] `PreConversionManager` 위치 확인 또는 신규 생성
- [ ] 현재 재생 중인 track 정보를 구독
- [ ] 사용자가 자주 선택할 voiceId 예측
  - 최근 선택한 voice
  - 현재 선택된 default voice
  - UI에서 hover/selection 중인 voice
- [ ] 재생 시작 후 N초 이내 preview pre-conversion 시작
- [ ] 이미 변환 중이면 중복 요청 방지
- [ ] 다른 곡으로 넘어가면 이전 pre-conversion 취소
- [ ] Wi-Fi / battery / app background 상태에 따른 제한 추가
- [ ] pre-conversion 완료 상태를 UI/Player에서 조회 가능하게 만들기

목표:

> Voice 버튼 클릭 시 서버 요청을 시작하는 게 아니라, 이미 준비된 preview를 바로 재생

---

### 1-4. Preview First, Full Track Later

- [x] Voice 버튼 클릭 시 full track 변환을 기다리지 않기
- [x] 먼저 15~60초 preview 변환본을 재생
- [x] full track 변환은 백그라운드에서 시작
- [x] full track이 준비되면 자연스럽게 교체
- [ ] 교체 시 playback position 맞추기
- [ ] preview 끝나기 전에 full track이 준비되지 않으면 fallback UX 정의
  - 원곡으로 잠시 돌아가기
  - loading 표시
  - preview loop 금지 여부 결정

목표:

> 사용자는 “기다리는 중”이 아니라 “이미 듣는 중”이라고 느끼게 만들기

---

### 1-5. Server-side Trim 검토

현재 iOS에서 오디오를 잘라서 export한다면 시간이 추가로 든다.

- [x] 현재 trim/export가 iOS에서 일어나는지 확인
- [x] 서버 API에 `startTime`, `duration` 파라미터를 넘길 수 있는지 확인
- [x] 가능하면 원본 URL + 구간 정보만 보내고 서버에서 trim
- [x] iOS local export 시간 제거

목표:

> iOS에서 파일 자르는 시간을 줄이고 Voice tap 이후 대기 감소

---

## Phase 2. Realtime Streaming PoC

Batch 최적화 후에도 Voice tap 대기가 충분히 줄지 않으면 realtime streaming을 검토한다.

### 2-1. Streaming 방식 결정

- [ ] WebSocket 사용 여부 결정
- [ ] HTTP chunked streaming 가능성 확인
- [ ] 서버가 chunk 단위 변환 결과를 바로 보내줄 수 있는지 확인
- [ ] iOS에서 chunk를 받는 즉시 buffer에 넣고 재생 가능한지 확인

후보:

```text
Client → Server: original audio chunk / PCM chunk
Server → Client: converted audio chunk
Client: jitter buffer에 쌓고 순서대로 재생
```

---

### 2-2. iOS Audio Playback 구조 검토

- [ ] 현재 AVPlayer 기반인지 확인
- [ ] realtime chunk playback에 AVAudioEngine이 필요한지 검토
- [ ] PCM buffer 재생 가능 구조 설계
- [ ] sample rate / channel count / bit depth 통일
- [ ] chunk buffer underflow 대응
- [ ] 네트워크 지연 시 fallback UX 정의

---

### 2-3. Chunking 정책

- [ ] chunk 길이 후보 테스트
  - 250ms
  - 500ms
  - 1s
  - 2s
- [ ] chunk가 너무 짧을 때 overhead 측정
- [ ] chunk가 너무 길 때 첫 소리 지연 측정
- [ ] overlap 길이 결정
- [ ] crossfade 적용 여부 결정

목표:

> 첫 소리는 빠르게, 경계는 끊기지 않게

---

### 2-4. Vocal Separation 문제 정리

Beam은 보통 다음 구조가 필요하다.

```text
원곡
→ 보컬/반주 분리
→ 보컬만 voice conversion
→ 반주와 remix
```

Realtime에서 어려운 점:

- Demucs 같은 separation은 앞뒤 문맥이 필요함
- chunk 단위로 자르면 품질이 떨어질 수 있음
- overlap 처리 때문에 같은 구간을 여러 번 처리해야 할 수 있음

TODO:

- [ ] realtime에서는 separation을 생략할 수 있는지 검토
- [ ] 미리 분리된 stem이 있는 경우만 realtime 지원할지 검토
- [ ] low quality realtime preview + high quality batch full version 조합 검토
- [ ] separation 서버 pipeline을 streaming-friendly하게 바꿀 수 있는지 확인

---

### 2-5. Backend / Infra 확인

- [ ] 사용자별 GPU session 유지가 가능한지 확인
- [ ] model warm-up 비용 측정
- [ ] 동시 접속자 수 대비 GPU 비용 계산
- [ ] streaming 중 취소/cancel 처리
- [ ] queue 방식과 session 방식 비교
- [ ] batch fallback 유지

---

## Phase 3. UX 정리

- [ ] Voice 버튼 상태 정의
  - Ready
  - Preparing
  - Playing Preview
  - Converting Full Track
  - Full Track Ready
  - Streaming
  - Failed
- [ ] Pre-conversion 완료된 voice는 즉시 선택 가능하게 표시할지 결정
- [ ] 사용자가 seek할 때 동작 정의
  - batch preview만 있는 경우
  - full track 변환 완료된 경우
  - realtime streaming인 경우
- [ ] 실패 시 원곡 재생 유지
- [ ] loading 문구를 기술 용어 없이 작성
  - 나쁜 예: `Batch conversion in progress`
  - 좋은 예: `Getting this voice ready...`

---

## 추천 구현 순서

1. 현재 conversion flow와 병목 측정
2. audio cache 추가
3. conversion result cache 추가
4. PreConversionManager 강화
5. preview first + full async 안정화
6. 서버 trim 도입 검토
7. 그래도 느리면 realtime streaming PoC 시작

---

## 성공 기준

### Short-term

- [ ] Voice 버튼 클릭 후 첫 변환 음원 재생까지 1초 이하인 케이스 만들기
- [ ] pre-conversion hit rate 50% 이상 목표
- [ ] cache hit 시 즉시 재생

### Mid-term

- [ ] preview는 빠르게 재생하고 full track은 백그라운드에서 자연스럽게 교체
- [ ] 사용자가 batch conversion을 기다린다고 느끼지 않게 만들기

### Long-term

- [ ] realtime streaming PoC에서 첫 chunk 0.5~2초 내 재생
- [ ] chunk 경계 끊김 최소화
- [ ] seek / cancel / network delay 대응

---

## 한 줄 정리

지금 당장 목표는 완전 realtime으로 갈아엎는 것이 아니라, **pre-conversion + preview + cache**로 Voice 버튼을 눌렀을 때 거의 바로 바뀐 목소리가 들리게 만드는 것이다. 이후에도 충분히 빠르지 않으면 WebSocket 기반 realtime streaming을 별도 R&D로 진행한다.
