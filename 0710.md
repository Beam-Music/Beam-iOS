# 2026-07-10 Beam iOS: RunPod GPU + E2E + UI 연동 잔여 작업

## 현재 상태

### RunPod GPU 서버
- `213.173.102.23:37723` — 인스턴스 다운됨 (SSH/HTTP 모두 연결 불가)
- RunPod 웹 콘솔에서 pod 재생성 필요 (SSH over TCP 새 IP/포트 확인)
- SSH 키: `~/.ssh/runpod_ed25519`
- 로컬 Nest 서버 (`:8080`)도 실행 중 아님

### 전체 진행 상황

| 단계 | 항목 | 상태 |
|------|------|------|
| Step 1 | RunPod GPU 활성화 (`gpu: false` → `cuda:0`) | ❌ RunPod 다운 |
| Step 2 | iOS E2E 검증 | ⏸️ RunPod + Nest 필요 |
| Step 3 | PreConversionManager UI 연동 | ⏸️ 미시작 |
| Step 4 | Preview→Full Track UX 완성 | ⏸️ 미시작 |

---

## Step 1: RunPod GPU 활성화 (가장 먼저)

### 1-1. RunPod Pod 재생성

RunPod 웹 콘솔 → Pods → 새 pod 생성:
- GPU: `A5000` / `A4000` / `RTX 3090` / `A10` (4090은 availability 낮음)
- 템플릿: 커스텀 (SSH만 켜기, Jupyter 끄기)
- 생성 후 **SSH over exposed TCP**의 IP/PORT 확인

```bash
# SSH 접속
ssh root@<NEW_IP> -p <NEW_PORT> -i ~/.ssh/runpod_ed25519

# CUDA 확인
nvidia-smi
python3 -c "import torch; print(torch.cuda.is_available())"
```

### 1-2. beam-svc-server 설정 및 실행 (RunPod 내부)

서버 코드가 이미 RunPod에 배포되어 있어야 함 (scp로 업로드 또는 git clone).

```bash
cd beam-svc-server

# .env 편집 (GPU 설정)
vim .env
```

`.env` 설정:
```bash
BEAM_RVC_REPO=/home/ubuntu/beam/beam-voice-conversion/Retrieval-based-Voice-Conversion-WebUI
BEAM_RVC_PYTHON=/home/ubuntu/beam/beam-voice-conversion/Retrieval-based-Voice-Conversion-WebUI/.venv/bin/python
BEAM_DEMUCS_COMMAND=demucs
BEAM_FFMPEG_COMMAND=ffmpeg
BEAM_FFPROBE_COMMAND=ffprobe
BEAM_RVC_DEVICE=cuda:0          # ← 변경: cpu → cuda:0
BEAM_RVC_IS_HALF=true           # ← 변경: false → true (fp16, VRAM 절약)
BEAM_RVC_F0_METHOD=rmvpe
```

```bash
# 서버 실행
uvicorn app.main:app --host 0.0.0.0 --port 8081

# health 확인
curl http://127.0.0.1:8081/ai-convert/health
# → "gpu": true 여야 함
```

### 1-3. Nest 게이트웨이 .env 업데이트 (로컬 Mac)

`beam-server-nest/.env`:
```bash
BEAM_SVC_URL=http://<NEW_IP>:8081
```

```bash
# Nest 재시작
cd beam-server-nest && npm run start:dev

# 확인
curl http://127.0.0.1:8080/ai-convert/health
curl http://127.0.0.1:8080/ai-convert/voices
```

### 1-4. GPU Smoke Test

```bash
# Nest 경유 preview 변환 (30초 trim)
BASE_URL=http://127.0.0.1:8080 \
TRIM_START=0 TRIM_DURATION=30 \
VOICE_ID=dionn_v1_singing VOICE_TYPE=singer \
OUT_FILE=/tmp/beam_gpu_smoke.mp3 \
beam-server-nest/scripts/smoke_test.sh /path/to/input.mp3

# 예상: CPU 4분 45초 → GPU 10~30초
```

---

## Step 2: iOS E2E 검증

### 사전 조건
- [ ] `beam-server-nest` 로컬 `:8080` 실행 중
- [ ] `BEAM_SVC_URL` = 새 RunPod IP
- [ ] PostgreSQL 실행 (`brew services start postgresql`)

### 테스트 A — Voice 버튼 → Preview 재생
1. Xcode → `Beam-iOS/BeamApp.xcodeproj` → iPhone Simulator
2. 앱 실행 → 데모곡 `Fix You` 재생
3. **Voice** 버튼 탭 → `dionn_v1_singing` 선택
4. 로딩 오버레이 확인
5. GPU 기준 10~30초 내 변환된 목소리 재생 시작
6. Xcode 콘솔 확인:
   - `🌐 Beam SVC convert URL: http://127.0.0.1:8080/ai-convert/voice-conversion`
   - `✂️ Using server-side preview trim...`
   - `📊 [TTFA] tap_to_audio_ms=...`

### 테스트 B — Full Track 교체
1. Preview 재생 중 자동 background full track 변환
2. Job polling → 완료 후 preview → full track 교체
3. `🎯 [Playback] Full track swap → seeked to ...s` 콘솔 확인
4. 교체 후 재생 위치가 preview 시점과 일치하는지 확인

### 테스트 C — Pre-conversion (로그인 필요)
1. 회원가입 → 이메일 인증 → 로그인
2. 곡 재생 시작 → `onChange(currentTrack)` → `preConversionManager.warmup()` 자동 호출
3. Voice 버튼 탭 시 cache hit → **즉시 재생**
4. 콘솔: `🎯 Telemetry Cache Hit!`

### 실패 시 체크리스트
| 증상 | 원인 |
|------|------|
| 음성 목록 fallback만 보임 | Nest `:8080` 미실행 또는 `/ai-convert/voices` 실패 |
| HTTP 500 / timeout | RunPod SVC down |
| pre-conversion 안 됨 | 미로그인 (`TokenStorage` nil) |
| 원곡과 소리 동일 | RVC stub/실패 — RunPod 로그 확인 |

---

## Step 3: PreConversionManager UI 연동

### 변경할 파일
- `BeamApp/Feature/Player/PlayerView.swift`

### 3-1. 마지막 선택 voice 저장/복원 확인
- `PreConversionManager.setPreferredVoice()` 이미 구현됨
- PlayerView에서 voice 선택 시 `preConversionManager.setPreferredVoice(voice)` 호출 확인 및 추가

### 3-2. Voice 선택 sheet에 pre-conversion 상태 badge
- voice 항목 옆에 상태 표시:
  - ✅ 준비 완료 (completed)
  - ⏳ 변환 중... (converting)
  - ❌ 실패 (failed)
- `preConversionManager.statusBadgeText(for: track)` 활용

### 3-3. Player 화면에 변환 상태 표시
- 현재 트랙의 pre-conversion 상태를 player 하단에 표시
- 완료된 곡: "Voice: Dionn · 변환 완료"
- 실패 시 재시도 버튼

### 3-4. 동일 곡 + 동일 voice 재요청 UX
- 이미 완료됨 → 즉시 재생 (이미 구현됨)
- 진행중 → 진행 표시 + 대기 (이미 구현됨)
- 실패 → 실패 메시지 + 재시도 (메시지 개선)

---

## Step 4: Preview → Full Track UX 완성

### 변경할 파일
- `BeamApp/Feature/Player/PlayerView.swift`
- `BeamApp/AudioProcessing/AudioManager.swift`
- `BeamApp/Feature/Player/PlayerReducer.swift`

### 4-1. Playback position 정밀 seek
- 현재: `Task.sleep(0.6s)` 후 seek → fragile
- 개선: `AVPlayer.seek(to:toleranceBefore:toleranceAfter:completionHandler:)` 사용
- Player가 준비된 후 첫 프레임에서 seek

### 4-2. Preview fallback UX
- GPU 활성화 후 full track은 10~30초면 완료 → preview 60초 내 거의 항상 도착
- 만약 미도착 시:
  - 원곡으로 자연스럽게 전환 (Voice 해제)
  - "전체 곡 준비 중..." 메시지 표시

### 4-3. Job progress 표시
- Full track async job의 progress (0~100%)를 UI에 표시

---

## 향후 확장 (장기)

### Phase 2: Realtime Streaming PoC
- WebSocket / HTTP chunked streaming 결정
- iOS AudioEngine 기반 chunk playback
- Chunk 정책 (250ms/500ms/1s) 및 crossfade

### Phase 3: UX 정리
- Voice 버튼 상태 정의 (Ready/Preparing/PlayingPreview/ConvertingFull/Streaming/Failed)
- Seek 동작 정의 (preview만 있는 경우 / full 완료 / streaming)
- 로딩 문구 자연어로 개선

### 기술부채
- PlayerView.swift (2529줄) 리팩토링
- RMVPE stub → 실제 F0 추출
- beam-svc-server job persistence (in-memory → disk/DB)
- Cache cleanup/eviction (tmp/cache/ 무한 증가)

---

## 내일 시작할 명령어

```bash
# 1. RunPod 접속
ssh root@<IP> -p <PORT> -i ~/.ssh/runpod_ed25519

# 2. RunPod에서 서버 실행
cd beam-svc-server && uvicorn app.main:app --host 0.0.0.0 --port 8081

# 3. 로컬 Nest 실행
cd beam-server-nest && npm run start:dev

# 4. PostgreSQL 실행 (로그인 필요시)
brew services start postgresql

# 5. Xcode 실행
open Beam-iOS/BeamApp.xcodeproj

# 6. 연결 확인
curl http://127.0.0.1:8080/ai-convert/health
curl http://127.0.0.1:8080/ai-convert/voices
```
