# 2026-07-13/14 Beam iOS: RunPod GPU 연결 재개 상태

## 확인 결과

- iOS 시뮬레이터 기본 API는 `http://127.0.0.1:8080`이다.
- `beam-server-nest/.env`는 `PORT=8080`, `BEAM_SVC_URL=http://213.173.110.44:35204`를 사용한다.
- 새 RunPod endpoint:
  - SSH: `root@213.173.110.44 -p 35203 -i ~/.ssh/runpod_ed25519`
  - HTTP: `http://213.173.110.44:35204` -> 내부 `:18081`
- RunPod GPU:
  - `NVIDIA RTX PRO 4500 Blackwell, 32623 MiB`
  - SVC venv torch CUDA: `True`
  - RVC venv torch CUDA: `True`
- Nest gateway 확인:
  - `GET http://127.0.0.1:8080/ai-convert/health` -> `{"ok":true,...,"gpu":true,"modelsLoaded":true}`
  - `GET http://127.0.0.1:8080/ai-convert/voices` -> 4 voices

## 현재 판정

iOS -> 로컬 Nest -> RunPod SVC 경로는 복구됐다. Health 기준 `gpu:true`이고, 실제 RVC 실행 명령도 `--device cuda:0 --is_half True`로 들어간다.

## RunPod 실행 상태

RunPod 내부 SVC 실행 명령:

```bash
cd /workspace/beamMusic/beam-svc-server
nohup .venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 18081 \
  </dev/null >/tmp/beam-svc-18081.log 2>&1 &
```

RunPod 코드 임시 패치:

- `app/services/pipeline.py`의 `health()`가 `"gpu": False`로 하드코딩되어 있어, `BEAM_RVC_DEVICE`가 `cuda`로 시작하면 true를 반환하도록 변경했다.
- 백업: `/workspace/beamMusic/beam-svc-server/app/services/pipeline.py.bak-0714`
- `app/services/demucs_service.py`에서 `BEAM_RVC_DEVICE`가 `cuda`로 시작하면 Demucs 명령에 `-d cuda`를 추가하도록 변경했다.
- 백업: `/workspace/beamMusic/beam-svc-server/app/services/demucs_service.py.bak-0714`

## Smoke Test

```bash
BASE_URL=http://127.0.0.1:8080 \
TRIM_START=0 TRIM_DURATION=3 \
VOICE_ID=dionn_v1_singing VOICE_TYPE=singer \
OUT_FILE=/tmp/beam_gpu_preview_3s.mp3 \
./scripts/smoke_test.sh /Users/anonymous/Desktop/Code/beamMusic/publish-songs/sza-levitating.mp3
```

결과: 성공, `/tmp/beam_gpu_preview_3s.mp3` 생성.

Demucs GPU 패치 후 추가 확인:

```bash
BASE_URL=http://127.0.0.1:8080 \
TRIM_START=0 TRIM_DURATION=10 \
VOICE_ID=dionn_v1_singing VOICE_TYPE=singer \
OUT_FILE=/tmp/beam_gpu_preview_10s_demucs_cuda.mp3 \
./scripts/smoke_test.sh /Users/anonymous/Desktop/Code/beamMusic/publish-songs/sza-levitating.mp3
```

결과: 성공, `/tmp/beam_gpu_preview_10s_demucs_cuda.mp3` 생성.

The Weeknd voice 추가:

- 모델 업로드 위치: `/workspace/beamMusic/beam-svc-server/weights/rvc/the_weeknd/`
  - `model.pth`
  - `added.index`
- RunPod registry에 `voiceId=the_weeknd`, `name=The Weeknd`, `voiceType=singer` 추가.
- registry 백업: `/workspace/beamMusic/beam-svc-server/model_registry/voices.json.bak-weeknd-0714`
- smoke test:

```bash
BASE_URL=http://127.0.0.1:8080 \
TRIM_START=0 TRIM_DURATION=3 \
VOICE_ID=the_weeknd VOICE_TYPE=singer \
OUT_FILE=/tmp/beam_weeknd_preview_3s.mp3 \
./scripts/smoke_test.sh /Users/anonymous/Desktop/Code/beamMusic/publish-songs/sza-levitating.mp3
```

결과: 성공, `/tmp/beam_weeknd_preview_3s.mp3` 생성.

주의:

- Demucs GPU 패치 후 10초 preview는 이전 수 분 대기에서 약 30초 내 성공으로 개선됐다.
- `nvidia-smi` 순간 샘플에서는 Demucs/RVC 프로세스를 잡지 못했다. 필요하면 `run_command()`에 명령 로깅을 추가해 실제 Demucs argv를 서버 로그에 남긴다.

## 다음 액션

1. iOS 시뮬레이터에서 Voice 버튼 -> `dionn_v1_singing` -> preview 재생을 확인한다.
2. Xcode 콘솔에서 다음 로그를 본다.

```text
Beam SVC convert URL: http://127.0.0.1:8080/ai-convert/voice-conversion
Using server-side preview trim...
```

3. preview 대기가 길면 RunPod에서 단계 확인:

```bash
ps -eo pid,etime,pcpu,comm,args | grep -E "demucs|infer_cli"
```

4. 다음 최적화 후보는 preview 전용 빠른 경로 또는 변환 progress 로깅이다.
