# 2026-07-14/15 Beam iOS 작업 재개 메모

## 오늘 확인한 상태

- 새 RunPod endpoint가 올라왔다.
  - SSH: `root@213.173.110.175 -p 36935 -i ~/.ssh/runpod_ed25519`
  - HTTP: `http://213.173.110.175:36936` -> 내부 `:18081`
  - Health: `{"ok":true,"gpu":true,"modelsLoaded":true}`
  - Voices: 4개 (`dionn_v1_singing`, `freya_idol`, `taylor_swift_singer`, `the_weeknd`)
- 현재 iOS 앱의 일반 API 서버는 Vapor `beam-server`가 맞다.
  - `Endpoints.baseURL` 기본값: `http://127.0.0.1:8080`
  - Vapor `beam-server/.env`: `PORT=8080`
  - Vapor의 `/ai-convert/*` route는 `BEAM_SVC_URL`로 SVC 서버에 proxy한다.
- 현재 iOS 앱의 직접 voice conversion 호출은 `Endpoints.beamSVCBaseURL`을 사용한다.
  - 기본값: `http://127.0.0.1:8081`
  - `VoiceConversion.list/convert/jobs/results`는 이 값을 기준으로 호출한다.
- 이전 확인 중 `beam-server-nest`를 켠 것은 잘못된 경로였다. 이 프로젝트의 현재 기준은 Vapor `beam-server` + Beam SVC다.
- Vapor `beam-server/.env`와 `/Users/anonymous/Documents/Beam-Server/.env`의 `BEAM_SVC_URL`을 `http://213.173.110.175:36936`로 갱신했다.
- 현재 실행 중인 repo 서버의 `JWT_SECRET`도 기존 Xcode 서버와 같은 `beam_music_local_jwt_secret_key_1234567890`로 맞췄다. 기존 iOS 로그인 토큰이 signature mismatch로 깨지는 것을 피하기 위함.
- 현재 repo의 Vapor 서버를 실행 중이다.
  - cwd: `/Users/anonymous/Desktop/Code/beamMusic/beam-server`
  - command: `.build/debug/App serve --hostname 0.0.0.0 --port 8080`
  - local health: `http://127.0.0.1:8080/ai-convert/health` -> `gpu:true`
- iOS Debug generic build는 성공했다.
  - 신규 error 없음
  - 기존 Swift 6 concurrency/deprecation warning은 남아 있음

## iOS 쪽 반영

- `BeamApp/Feature/Player/PlayerView.swift`
  - Voice 버튼 옆에 `Prepare` 버튼 추가
  - `Prepare`는 `voiceSelectionMode = .preconvert`로 voice sheet를 열고, 선택한 voice를 `PreConversionManager.enqueue(track:voice:)`에 연결
  - 현재 트랙의 pre-conversion 상태 패널 추가
    - `Voice: <voice> · Queued/Converting/Done/Failed`
    - 실패 시 error message 1줄 표시
    - 실패 상태에서 `Retry` 버튼으로 같은 voice 재요청

## 현재 막힌 부분

RunPod SVC와 Vapor proxy는 복구됐다. 이제 iOS 시뮬레이터에서 `Prepare` 또는 `Voice` 경로를 다시 테스트하면 된다.

## 다음 액션

1. RunPod 새 endpoint 확인
   - SSH over TCP IP/PORT
   - HTTP exposed port -> 내부 SVC `:18081`
2. Vapor `beam-server/.env`의 `BEAM_SVC_URL`을 새 HTTP endpoint로 교체
3. 필요하면 iOS `BEAM_SVC_BASE_URL`도 같은 endpoint 또는 로컬 SVC proxy로 맞춘다.
4. 로컬 Vapor server health 확인

```bash
curl http://127.0.0.1:8080/ai-convert/health
curl http://127.0.0.1:8080/ai-convert/voices
```

5. smoke test 재실행

```bash
BASE_URL=http://127.0.0.1:8080 \
TRIM_START=0 TRIM_DURATION=3 \
VOICE_ID=dionn_v1_singing VOICE_TYPE=singer \
OUT_FILE=/tmp/beam_gpu_preview_3s.mp3 \
/Users/anonymous/Desktop/Code/beamMusic/beam-svc-server/scripts/smoke_test.sh \
/Users/anonymous/Desktop/Code/beamMusic/publish-songs/sza-levitating.mp3
```

6. iOS 시뮬레이터에서 확인
   - Voice -> preview 변환/재생
   - Prepare -> 상태 패널 Queued/Converting/Done/Failed
   - Failed -> Retry
