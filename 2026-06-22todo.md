# 2026-06-22 TODO

## 오늘 한 일 요약
- 로그인 요청 경로 수정 및 확인 완료
  - iOS App -> `http://127.0.0.1:8080`
  - Beam SVC -> `http://127.0.0.1:8081`
- LALAL 경로 제거
- Nest `ai-convert`를 자체 `beam-svc-server` 프록시로 변경
- 미니 플레이어 가수명 색상 검은색으로 수정
- macOS 로컬 RVC 실행은 계속 실패
  - segfault / numba / RVC runtime 문제
  - 결론: Linux GPU 서버에서 돌리기로 결정
- Runpod 4090 pod 생성/SSH 접속 성공
- 하지만 GPU instance availability 문제로 pod 유지 실패

## 수정된 주요 파일
- `BeamApp/Info.plist`
- `BeamApp/Network/Endpoints.swift`
- `BeamApp/Feature/MiniPlayer/MiniPlayerView.swift`
- `BeamApp/Feature/Home/HomeView.swift`
- `BeamApp/Feature/Player/PlayerView.swift`
- `BeamApp/Network/VoiceConversionClient.swift`
- `../beam-server-nest/src/ai-convert/ai-convert.service.ts`
- `../beam-server-nest/src/ai-convert/ai-convert.controller.ts`
- `../beam-server-nest/.env.example`
- `../beam-server-nest/README.md`
- `docs/remote-svc-deployment.md`
- `/Users/anonymous/Desktop/Code/beamMusic/beam-voice-conversion/Retrieval-based-Voice-Conversion-WebUI/tools/cmd/infer_cli.py`

## 현재 로컬 상태
- Nest API 서버 포트: `8080`
- Beam SVC 서버 포트: `8081`
- Nest env에서 나중에 설정할 값:
  - `BEAM_SVC_URL=http://<RUNPOD_IP>:8081`
- 앱은 계속 Nest만 보도록 유지

## 집에 가서 다시 할 일
1. Runpod에서 새 Pod 다시 만들기
   - 4090 말고 빈 GPU 우선
   - 추천: `A5000`, `A4000`, `A4500`, `RTX 3090`, `A10`
   - Jupyter 끄기
   - SSH만 켜기
2. Pod 상세 페이지에서 `SSH over exposed TCP`의 새 IP/포트 확인
3. SSH 접속
   - `ssh root@<IP> -p <PORT> -i ~/.ssh/runpod_ed25519`
4. 서버 상태 확인
   - `nvidia-smi`
   - `python3 --version`
   - `git --version`
   - `ffmpeg -version | head -1`
5. 로컬 Mac에서 코드 업로드
   - `beam-svc-server`
   - `beam-svc-server/weights`
   - `beam-voice-conversion/Retrieval-based-Voice-Conversion-WebUI` (logs 제외)
6. Runpod 서버에서 Python 3.10 설치
7. RVC WebUI `.venv` 설치
8. `beam-svc-server` `.venv` 설치 및 `.env` 설정
9. 원격 서버에서 `uvicorn app.main:app --host 0.0.0.0 --port 8081` 실행
10. 로컬 Mac의 `beam-server-nest/.env`에
    - `PORT=8080`
    - `BEAM_SVC_URL=http://<RUNPOD_IP>:8081`
11. 로컬 Nest 재실행
12. 최종 확인
    - `curl http://127.0.0.1:8080/ai-convert/health`
    - `curl http://127.0.0.1:8080/ai-convert/voices`
    - 앱에서 음성 변환 테스트

## 다음에 나에게 바로 말할 문장
- "Runpod 새 Pod 만들었고 원격 GPU 배포 이어서 하자"
- 또는
- "원격 Linux GPU 서버 배포 체크리스트 이어서 하자"
