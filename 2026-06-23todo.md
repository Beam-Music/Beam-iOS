# 2026-06-23 TODO

## 현재 문제
1. **변환 시간이 재생 시간보다 김**
   - 2분 30초짜리 곡도 변환에 4분 이상 걸림
   - 현재는 재생 중인 곡을 바로 변환하는 흐름이라, 노래가 끝난 뒤 결과를 기다려야 함

2. **변환 결과 재사용이 안 됨**
   - 한 번 변환한 노래를 따로 보관/재생하는 흐름이 없음
   - 다시 들으려면 매번 다시 변환하거나 접근 경로가 불명확함

## 목표
- 재생 중인 현재 곡이 아니라 **재생목록(플레이리스트/큐)에 있는 곡을 미리 변환**할 수 있게 한다.
- **한 번 변환된 곡은 저장**해서 이후에도 계속 들을 수 있게 한다.

## 해야 할 일

### 1) UX / 플로우 정리
- [ ] 현재 재생곡 기준 변환 UX를 재생목록 기준으로 변경
- [ ] 곡 상세/플레이어/재생목록에서 "이 곡 변환" 진입점 정의
- [ ] 변환 상태 표시 설계
  - 대기중
  - 변환중
  - 완료
  - 실패
- [ ] 변환이 끝난 곡을 어디서 다시 들을지 화면 정의
  - 변환 보관함
  - 사용자 플레이리스트 내 별도 섹션
  - 곡 상세의 변환본 목록

### 2) iOS 앱 작업 (`Beam-iOS`)
- [ ] 재생 중인 곡 외에 **큐/플레이리스트의 임의 곡 변환 요청** 가능하게 구조 변경
- [ ] 변환 job polling / status tracking UI 추가
- [ ] 변환 완료 후 결과 곡 메타데이터를 로컬 state/store에 반영
- [ ] "변환된 곡" 목록/탭/섹션 추가
- [ ] 변환 완료 곡 재생 로직 추가
- [ ] 동일 곡+동일 voice 조합이면 중복 변환 막기
- [ ] 백그라운드/앱 재실행 후에도 변환 상태 복원할지 결정

### 3) Beam-Server 작업 (`Beam-Server`)
- [ ] 변환 요청을 **현재 재생 흐름과 분리된 비동기 job**으로 관리
- [ ] 변환 결과 메타데이터 저장 API 정의
- [ ] 사용자별 변환 결과 조회 API 추가
- [ ] 동일 입력 곡 + 동일 voice 변환 결과 재사용 정책 정의
- [ ] 변환 완료된 파일의 playback URL 제공 방식 정리

### 4) Beam SVC 서버 작업 (`beam-svc-server`)
- [ ] 긴 변환 작업을 job 기반으로 안정적으로 처리
- [ ] job id / status / result endpoint 확실히 정리
- [ ] 결과 파일 저장 위치/보존 기간 정책 정리
- [ ] 같은 요청 재실행 시 캐시 재사용 가능 여부 검토

### 5) 데이터 모델 / 저장 정책
- [ ] 저장해야 할 정보 정의
  - 원본 곡 ID
  - 사용자 ID
  - voice ID
  - 변환 상태
  - 결과 파일 URL
  - 생성 시각
- [ ] 변환 결과를 `ai-song`으로 볼지, 별도 `converted-song` 개념으로 둘지 결정
- [ ] 서버 재시작/앱 재설치 후에도 다시 들을 수 있도록 저장 범위 결정

### 6) 우선 구현 순서 제안
- [ ] 1차: 재생목록의 곡을 미리 변환 요청 가능하게 만들기
- [ ] 2차: 변환 job 상태 조회 및 완료 UI 붙이기
- [ ] 3차: 변환 결과 저장/목록화/재생 기능 추가
- [ ] 4차: 중복 변환 방지 및 캐시 최적화

## 구현 메모
- 핵심은 "실시간 변환해서 바로 듣기"보다,
  **미리 변환 요청 → 완료되면 저장 → 나중에 재생** 흐름으로 바꾸는 것
- 변환 시간을 고려하면 재생 중 즉시 결과를 기대하는 UX는 맞지 않음
- 사용자 입장에서는 "변환 작업을 걸어두고, 완료된 곡을 컬렉션처럼 다시 듣는 경험"이 더 적합함

## 다음 작업 시작 문장
- "2026-06-23 todo 기준으로 재생목록 곡 선변환 구조부터 설계하자"
- 또는
- "변환 결과 저장용 API/모델부터 정리하자"
